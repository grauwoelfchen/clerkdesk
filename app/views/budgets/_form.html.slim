= form_for budget, url: finance_budget_path(finance), html: {class: "form pure-form"} do |f|
  - if budget.errors.any?
    = render "shared/error", resource: budget
  fieldset.pure-group
    = f.text_field :title,  class: "pure-input-1", placeholder: "A title"
  .action
    = f.submit t("general.#{action}"), class: "pure-button button"
    - if action == "update"
      = link_to t("general.destroy"), finance_budget_path(finance), class: "pure-button button button-error", data: {method: :delete, confirm: "Are you sure?"}
